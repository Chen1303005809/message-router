# 客户问题事件中心

## 咨询人员与研发人员使用手册

版本：MVP（以 2026-09-21 仓库实现为准）

## 1. 手册说明

本系统通过企业微信智能机器人，把咨询人员提交的客户问题转交给指定研发人员，并保留一条连续、可追溯的事件时间线。

系统的基本协作链路是：

```text
H5 主页或咨询单聊机器人 → 创建事件 → 研发责任团队群 → 研发正式反馈 → 咨询确认客户闭环
```

系统中一件可独立负责、独立关闭的问题对应一个“事件”。事件在人员交接、研发换人或研发群迁移后仍保持同一个事件编号和完整历史。

## 2. 使用前准备

使用前请确认：

1. 你的企业微信账号已经由事件中心管理员录入，并处于“在岗”状态。
2. 咨询人员已被加入对应的咨询队列；研发人员已被加入对应的研发责任团队。
3. 研发责任团队已经绑定一个有效的企业微信群。
4. 研发群已添加机器人并完成绑定；如需接收咨询群副本，也需将机器人加入咨询群并绑定该队列。如需使用机器人草稿创建方式，咨询人员还需能单聊机器人。
5. H5 事件中心可以从企业微信内打开。生产环境使用企业微信身份登录，不需要另设密码。

如果看不到事件、无法创建事件或无法打开详情，优先联系事件中心管理员检查成员授权和团队归属。

## 3. 重要概念和状态

| 名称 | 含义 |
| --- | --- |
| 事件 | 一件可独立负责、独立关闭的客户问题。 |
| 事件编号 | 机器人事件消息中的 `〔KF·XXXXXXX〕` 标记，用于把后续消息关联到同一事件。无需手工输入。 |
| 咨询队列 | 咨询侧的组织归属。同一队列的在岗成员通常可以查看该队列的事件。 |
| 当前咨询经办人 | 当前负责推进事件、接收机器人通知的咨询人员。交接后立即变更。 |
| 研发责任团队 | 当前承担研发责任的团队，对应一个研发群。 |
| 当前研发处理人 | 当前被指定推进事件的研发人员。其他研发成员可以代为回复，但不会自动变成处理人。 |
| 正式消息 | 明确交给机器人、需要跨咨询侧和研发侧传递的消息。 |
| 仅同步进展 | 把消息同步给对方，但不把下一步责任转给对方。 |

事件中心常见状态如下：

| 页面状态 | 说明 | 下一步 |
| --- | --- | --- |
| 等待研发 | 咨询已提交或补充了问题，等研发处理。 | 研发分析并正式回复。 |
| 等待咨询 | 研发已反馈，等咨询向客户确认、补充信息或继续跟进。 | 咨询处理客户侧后续。 |
| 已关闭 | 咨询已确认客户侧完成闭环。 | 如问题未解决，可重新打开。 |
| 处理中 | 事件仍打开，但当前没有明确的等待方。 | 打开详情查看最新时间线。 |

注意：研发提交解决方案不会自动关闭事件，只有咨询确认客户侧闭环后才能关闭。

## 4. 咨询人员使用说明

### 4.1 创建新事件

可以直接从 H5 主页创建，也可以继续使用机器人草稿：

**直接从 H5 创建**

1. 打开 H5 事件中心主页，点击“新建事件”。
2. 填写事件标题、问题描述和客户 / 企业名称；联系人和联系方式可选。
3. 可一次选择多张问题图片，单张不超过 20 MiB；问题描述文字必填。
4. 选择紧急程度、咨询队列和研发处理人，点击“创建并发送给研发”。

**从机器人草稿创建**

1. 在企业微信中单聊智能机器人，发送客户问题。可以发送纯文字，也可以发送“文字 + 图片”的图文混排内容。
2. 一条正式消息至少要包含一段文字；仅发送图片目前不能创建正式事件。
3. 机器人返回“消息草稿已保存”后，点击“创建新事件”。
4. 在 H5 页面填写：
   - 事件标题：简短描述问题，例如“订单提交后页面报错”；
   - 客户 / 企业名称：必填；联系人和联系方式可选；
   - 紧急程度：普通、紧急或严重，默认普通；
   - 咨询队列：选择问题所属的咨询队列；
   - 研发处理人：从列表中选择明确的研发人员。
5. 检查原始消息和图片顺序无误后，点击“创建并发送给研发”。

无论使用哪种方式，创建成功后都可以在 H5 事件详情查看完整时间线。转发消息会展示事件标题和发言人，图片作为附件发送；“查看事件历史”通过企微模板卡片按钮跳转到对应详情，而不是正文中的 Markdown 外链。未登录时会先完成企业微信身份验证，再返回该事件详情。

事件截止时间默认是创建后 3 小时；临近期限提醒默认提前 24 小时，因此新建事件会立即显示“临近期限”。咨询人员可在事件详情中按 30 分钟步进提前或延后截止时间，并调整临近期限提醒时间。

建议在问题描述中补充以下信息：

- 客户或业务背景；
- 发生时间、环境、版本和账号范围；
- 实际现象与预期结果；
- 可复现步骤；
- 影响范围和紧急程度；
- 截图或必要的日志片段。

涉及客户隐私、密钥或内部敏感信息时，请先按公司信息安全规范脱敏。

### 4.2 查看自己的事件

有三种入口：

1. 点击机器人回复中的“事件中心”按钮；
2. 从企业微信工作台打开事件中心；
3. 在机器人单聊中发送 `我的事件`，点击“打开事件中心”。

事件列表提供以下筛选：

- **待咨询处理**：下一步需要咨询侧处理的开放事件；
- **等待研发**：下一步需要研发侧处理的事件；
- **全部打开**：所有未关闭事件；
- **已关闭**：已经完成闭环的事件。

主页的数据一览显示当前账号按团队权限可见的进行中、待咨询处理、等待研发和已关闭事件数量。新建入口也只对有咨询队列权限的成员显示。

打开事件详情可以查看客户资料、紧急程度、截止时间、临近期限提醒设置、当前状态、双方经办人和完整图文时间线。开放事件剩余时间进入提醒阈值后显示“临近期限”，到达截止时间后显示“已超时”。提醒默认提前 24 小时，咨询人员可按 30 分钟递增调整；截止时间提前或延后后都会追加到时间线。

### 4.3 给已有事件补充信息

#### 方式一：在机器人单聊中引用

1. 找到机器人之前发出的、包含 `〔KF·XXXXXXX〕` 的普通事件消息。
2. 引用该普通消息，而不是只引用图片。
3. 在引用回复中补充内容并发送。

机器人会根据引用中的事件标记，把消息追加到同一事件，并默认交给研发处理。

#### 方式二：在 H5 事件详情中发送

1. 打开目标事件详情。
2. 在“发送正式消息”区域填写补充内容。
3. 直接点击“发送”，表示补充信息并交给研发。
4. 如果只是同步进展、不希望改变当前等待方，勾选“仅同步进展”后再发送。

当前 H5 表单主要用于发送文字；需要补充图片时，优先在机器人单聊中引用事件文字消息后发送图文混排。

不要发送没有引用、也没有明确事件入口的普通消息来“碰运气”关联最近事件。系统不会默认把它归到最近一次事件，以免串单。

### 4.4 接收研发反馈并继续处理

研发正式回复后，机器人会私聊当前咨询经办人；所属咨询队列绑定了咨询群时，也会向群内发送副本。经办人私聊送达后，事件通常显示为“等待咨询”。

咨询人员收到反馈后可以：

- 将解决方案交付给客户；
- 根据研发反馈向客户补充确认；
- 引用该事件消息继续向研发提问；
- 在 H5 中追加正式消息；
- 在必要时把事件交接给另一位咨询人员。

### 4.5 交接给另一位咨询人员

咨询交接必须显式执行，不会因为其他人查看、评论或转发事件而自动发生。

交接规则：

- 当前咨询经办人或咨询队列管理员可以发起交接；
- 接收人必须是同一咨询队列中的在岗成员；
- 交接立即生效，不需要接收人确认；
- 原事件编号和完整时间线保持不变；
- 新经办人会收到交接通知和事件中心入口。

如果当前部署的 H5 页面没有显示交接控件，请联系咨询队列管理员或系统管理员处理，不要通过新建重复事件代替交接。

### 4.6 关闭和重新打开事件

#### 关闭事件

只有在客户侧已经确认问题解决、无需继续跟进时，才执行关闭：

1. 打开事件详情；
2. 确认时间线中已有研发结论和客户侧处理结果；
3. 点击“确认客户侧闭环并关闭”。

关闭后，事件进入“已关闭”，等待方为“无”。研发群只会收到一条关闭通知。

通常只有当前咨询经办人、咨询队列管理员或总管理员可以关闭事件。

#### 重新打开事件

以下情况可以重新打开原事件：

- 原问题实际上没有解决；
- 客户验证失败，需要研发继续处理；
- 需要补充原事件的处理过程。

重新打开时必须明确选择下一步等待“研发”还是“咨询”。

如果问题已经解决，后来因为新的版本、环境或现象再次发生，建议新建关联事件，而不是无限追加到旧事件；如当前页面没有关联事件入口，请联系管理员。

## 5. 研发人员使用说明

### 5.1 接收研发任务

新事件、咨询补充和研发团队转入通知会发送到当前研发责任团队绑定的企业微信群。研发团队成员也可以从 H5 事件中心查看当前团队有权限访问的事件和完整时间线。

事件消息通常包含：

- 事件标题、事件编号和当次发言；
- 发言人姓名及咨询侧 / 研发侧信息；系统通知会显示操作人；
- 可点击的“查看事件历史”企微模板卡片按钮；
- 咨询转发到研发群时，对当前研发经办人的 @ 提醒；
- 图文消息中的图片附件。

卡片跳转地址使用已配置的 H5 详情页，并沿用企业微信 OAuth 登录回跳。部署时需确认 H5 域名已加入企业微信应用的可信域名，并在企微客户端分别验证卡片在移动端和桌面端是否留在企微内打开；客户端的打开策略不能仅靠服务端 URL 保证。

研发群中的普通内部讨论不会进入事件时间线，也不会被机器人转发。只有明确 `@机器人` 的正式回复才会跨端传递。

### 5.2 正式回复的正确操作

在研发群中正式回复时，必须同时完成以下动作：

1. **引用机器人发送的普通事件消息**。不要只引用图片。
2. **@智能机器人**。不 @ 机器人时，系统不会把消息作为正式反馈处理。
3. 发送文字，或发送“文字 + 图片”的图文混排回复。
4. 回复至少包含一段文字；仅引用图片、仅 @ 机器人或没有引用，都会被拒绝或忽略。

推荐格式：

```text
（引用机器人发送的事件文字消息）
@客户问题机器人

现象确认：
原因定位：
处理结果：
修复版本 / 临时方案：
验证方式：
风险或待确认事项：
```

机器人发出的普通事件消息会带有事件标记。研发人员不需要复制或手工输入标记，只要正确引用事件消息即可。

### 5.3 “回复并交给咨询”与“仅同步进展”

研发群中的普通正式回复默认表示“回复并交给咨询”。回复会私聊当前咨询经办人；咨询队列已绑定群聊时，也会把文字和图片副本发送到该群。经办人私聊送达后，事件切换为“等待咨询”；咨询群副本独立重试，不阻塞案件流转。

如果只是同步当前排查进展、不希望把下一步责任转给咨询，请在 H5 事件详情中发送，并勾选“仅同步进展”。该消息仍会追加到时间线并发送给咨询经办人；队列已绑定群聊时也会发送群副本，但等待方保持不变。

### 5.4 研发代为回复与研发转交

同一研发责任团队的其他成员可以代为正式回复。代为回复会记录实际回复作者，但不会自动改变“当前研发处理人”。

需要更换责任人时，应执行显式的研发转交：

- 当前咨询经办人、当前研发处理人或研发团队管理员可以发起；
- 新处理人必须是当前研发责任团队的在岗成员；
- 事件编号和历史不变；
- 新处理人会收到通知。

如果需要转到另一个研发责任团队，应执行研发团队转交，而不是新建一个事件。转交后，原团队不再接收后续更新，新团队会收到转入通知和事件中心入口。

当前仓库版 H5 页面可能未显示研发转交按钮；遇到人员变更时请联系研发团队管理员或系统管理员处理。

### 5.5 群聊绑定与更换

每个咨询队列可绑定一个咨询群。先将机器人加入目标群，再由咨询队列管理员发送 `@客户问题机器人 绑定咨询队列 华东咨询队列`；总管理员也可以在 H5“组织与授权”页面录入群聊标识并绑定。

- 绑定后，研发回复会同时发送给当前咨询经办人和该队列群成员；
- 未绑定时仍按现有流程私聊经办人，绑定前已发送的回复不会补发；
- 重新绑定后，新回复发往新群，已排队的投递仍发往创建时确定的群；
- 研发群重新绑定只改变后续研发群路由，不改变事件的研发责任团队。

## 6. 团队管理员的准备和维护

团队管理员通常不负责日常每条消息的处理，但需要保证路由可用：

1. 确认成员已录入事件中心，并且企业微信 userid 无误。
2. 将咨询人员加入正确的咨询队列，将研发人员加入正确的研发责任团队。
3. 在目标群中发送对应绑定指令：

   ```text
   @客户问题机器人 绑定研发团队 平台研发组
   @客户问题机器人 绑定咨询队列 华东咨询队列
   ```

   研发团队或咨询队列名称必须与管理中心中的名称完全一致。群聊绑定仅允许对应团队管理员或总管理员执行。

4. 在 H5“组织与授权”页面确认对应团队显示为已绑定群聊；总管理员也可以直接在该页面维护群聊标识。
5. 人员离岗或调岗时，先停用或移除其团队权限，再显式处理仍在进行中的事件交接。

研发人员必须有且只能有一个有效的研发责任团队和一个已初始化的研发群路由，系统才会允许向其创建新事件。

## 7. 常见问题处理

| 现象 | 原因和处理方式 |
| --- | --- |
| 机器人没有回应咨询单聊 | 确认发送的是文字或图文混排；正式消息必须至少有一段文字。图片、文件、语音和视频等独立消息当前不属于 MVP 支持范围。必要时重新发送。 |
| 机器人没有回应研发群消息 | 确认已 `@机器人`，并且引用了机器人发出的普通事件消息，而不是只引用图片。 |
| 提示无法识别事件 | 引用内容中没有唯一有效的事件标记。请重新引用同一事件的机器人文字消息，或从 H5 事件中心打开目标事件后发送。 |
| 事件没有出现在列表中 | 检查当前企业微信身份是否为在岗成员，以及是否属于该事件的咨询队列或当前研发责任团队。 |
| H5 提示没有权限 | 事件可能已转到其他研发责任团队，或你的成员/团队授权已失效。联系管理员确认。 |
| 消息长时间没有到达对方 | 系统会先保存事件，再异步投递；图片较多时可能需要等待。不要连续重复发送，联系管理员检查投递状态。 |
| 关闭或发送时提示状态已变化 | 其他人已经更新了事件。刷新事件详情，确认最新时间线后再操作。 |
| 想把消息从一个事件移到另一个事件 | 不要删除或覆盖历史。由有权限的咨询管理员执行追加式纠错，原事件会保留误发记录，目标事件追加正确消息。 |
| 研发群里讨论很多，但时间线没有记录 | 未 `@机器人` 的群内讨论属于内部讨论，不会进入事件历史；需要跨端传递时请按正式回复规则操作。 |

## 8. 使用规范速查

### 咨询人员

- 一件独立问题对应一个事件；
- 新问题可从 H5 主页创建，也可先发给机器人再通过“创建新事件”归类；
- 跟进旧问题时引用事件文字消息；
- 图文消息保持问题描述和截图顺序，至少包含文字；
- 研发回复后由咨询确认客户闭环；
- 不要用新建事件代替人员交接。

### 研发人员

- 正式回复必须“引用机器人普通事件消息 + @机器人”；
- 只引用图片或不引用不能完成事件归类；
- 普通正式回复表示交给咨询；
- 进展同步请在 H5 勾选“仅同步进展”；
- 代为回复不会自动改变当前研发处理人；
- 内部讨论不需要 @机器人，也不会进入事件时间线。

## 9. 当前 MVP 边界

- 已支持文字和文字 + 图片的有序图文消息；
- 独立文件、语音、视频等消息暂不作为正式事件内容处理；
- H5 提供事件列表、权限范围数据一览、直接创建事件（文字和多图）、详情、文字发送、关闭和重开；
- 咨询交接、研发处理人转交、研发团队转交等后端能力已具备，但具体部署版本可能尚未显示对应的可视化按钮；
- 事件编号用于消息关联，不是权限凭证；能否查看和操作事件始终由企业微信身份及当前团队授权决定。
