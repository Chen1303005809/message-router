# 企业微信跨部门问题流转机器人：实施基线

最后更新：2026-09-21

访谈进度：第 40 问后停止 grilling，进入 [MVP 实施方案](./IMPLEMENTATION_PLAN.md)编写，不直接实现代码。

## 目标与边界

- 咨询人员通过机器人把客户问题发送给指定研发责任团队。
- 研发人员位于按研发部门划分的群聊中；群内只有研发人员和机器人。
- 研发完成处理后，通过机器人把结果返回给咨询侧。
- 不设计任务认领或抢单机制；咨询人员知道问题应该交给谁。
- 系统需要在咨询、研发人员变化后仍保持事件历史连续，并尽量保持群聊整洁。

## 已确认的产品决策

### Q1-Q5

1. **事件是一级实体。** 每个问题创建独立 `case_id`。消息引用是识别事件的入口之一，不是事件身份或数据归属的唯一事实来源。
2. **团队持有事件。** 咨询侧使用稳定的 `consult_queue_id` 表示长期归属；`current_consultant_id` 是可变的当前经办人；`creator_id` 仅用于审计。
3. **研发侧可见当前咨询经办人和历史消息作者。** 2026-09-04 修订：此规则取代此前的“研发侧统一匿名”。机器人消息和 H5 研发视图展示当前咨询经办人，每条历史咨询消息展示当时的真实作者。
4. **后台事件时间线是完整历史的事实来源。** 时间线采用追加式记录，至少包含原始输入、附件引用、真实操作者、跨端实际投递内容、引用关系、经办人变更、状态变化以及投递结果。
5. **咨询侧采用个人私聊加团队事件队列。** 个人与机器人私聊负责提交和接收；事件队列负责团队归属、查询和接续，不建立承载所有事件消息的咨询群。

### Q6-Q10

6. **交接时自动提供快照，不自动重放全部历史。** 快照包含事件编号、标题、状态、研发责任团队、已确认结论、阻塞点、下一步责任方、最近关键沟通以及完整时间线入口。
7. **只保留咨询个人经办人交接。** 当前咨询经办人或咨询队列、研发团队管理员可以转交咨询经办人；接收人必须是当前咨询队列在岗成员。研发侧不设置事件个人处理人，也没有个人研发交接。咨询经办人变更写入事件时间线，并私聊通知新经办人；若咨询队列绑定了咨询群也会向该群显式通知。
8. **转交立即生效。** 不设置“待接收”状态，也不要求新经办人确认。
9. **不设置交接超时或未处理告警。** 当前组织条件是相关人员工位相近，交接通过口头沟通保障；不要在后续方案中重新引入确认或告警流程。
10. **机器人不得猜测消息归属。** 无法唯一确定 `case_id` 时不得跨端转发，必须提示发送人重新引用、提供事件编号或选择事件。

### Q11-Q15

11. **归类与对外发送是两个动作。** 研发只有显式面向机器人发送的内容才作为正式咨询反馈跨端转发。在群聊交互中采用“引用携带事件标记的相关消息并 `@智能机器人`”作为显式动作。未 `@智能机器人` 的研发内部讨论不会触发回调，机器人不接收、不记录也不转发。
12. **每个事件同时具有内部 ID 和消息关联标记。** 后台使用内部 UUID；机器人发出的每一条可被引用的消息都携带唯一短编号。短编号仅供机器解析，人员不需要理解、记忆或手工输入，展示时应弱化处理。
13. **完整历史有明确边界。** 完整事件历史包括所有经过机器人的跨端沟通，以及转交、状态变化等系统事件；不包括研发群内未 `@智能机器人` 的内部讨论。
14. **智能机器人采用长连接模式作为主通道。** URL 回调模式的临时回复能力不足以完成跨会话路由。相关咨询用户和研发群需要先与机器人发生一次交互，系统随后保存可主动推送的用户及群会话映射。
15. **按研发责任团队路由。** 咨询侧选择已绑定研发群的团队，后台维护“研发团队 → 当前研发群 `chatid`”映射。团队负责人展示名是纯文本，不绑定企业微信身份、不授予权限、不参与路由。

### Q16-Q20

16. **新建事件与跟进旧事件都必须显式表达。** 咨询人员通过“新建事件”入口选择研发责任团队并提交内容；跟进旧事件时引用该事件的机器人消息。无引用的普通私聊消息由机器人询问“新建还是选择已有事件”，不得默认使用最近事件。
17. **研发完成不等于事件关闭。** 研发提交解决结果后，事件进入“待咨询确认”；只有咨询确认已向客户交付且无需继续跟进后，事件才正式关闭。
18. **已关闭事件的后续反馈由咨询显式归类。** 原问题未解决时重新打开原事件；问题解决后再次发生，或环境、版本、现象明显变化时新建关联事件，并以 `related_case_id` 关联原事件。机器人不能自动替咨询决定。
19. **研发侧仅以团队持有新事件。** `dev_team_id` 表示当前研发责任团队并与研发群绑定；应用不再读取或设置事件级研发经办人。为兼容旧库，已有的 `current_developer_id` nullable 列保留但不参与业务。研发团队可配置一个纯文本负责人展示名，但它不是成员关联。
20. **研发侧没有个人经办人交接。** 研发团队成员通过团队权限协作处理；正式回复记录实际发送人，但不会指定或变更某个个人处理目标。已绑定研发群内的任意成员（包括尚未录入事件中心的成员）都可以引用事件并回复；这类群内回复不因此获得 H5 查看或管理权限。需要改变研发归属时执行研发团队转交。

### Q21-Q25

21. **跨研发团队转交不新建事件。** 系统保留同一个 `case_id` 并同步变更当前研发团队；原研发群收到简短转出消息并停止接收后续更新，新研发群收到团队转入通知。新旧团队归属变化写入时间线。因此研发团队归属是受控可变字段。
22. **研发群采用低噪声消息策略。** 只发送新事件、咨询补充、转入等需要研发关注的内容；事件关闭或转出时只发送一条简短状态消息；研发正式反馈转发成功后保持静默；只有归属不明或转发失败等异常才在群中提示。
23. **聊天承担通知和快捷操作，H5 事件中心承担完整工作界面。** 咨询私聊只展示摘要、最新进展和入口；企业微信内打开的 H5 页面提供完整时间线、附件、状态、责任方、交接记录、关联事件和生命周期操作。页面必须根据企业微信身份和团队权限鉴权，链接本身不授予权限。
24. **事件中心采用双入口。** 每条事件卡片提供直达详情的深链接；企业微信工作台中的自建应用提供稳定的事件队列入口。私聊机器人发送“我的事件”只作为快捷入口。
25. **咨询队列决定可见范围，经办人只决定责任和通知。** 同一 `consult_queue_id` 下的所有在岗咨询人员均可查看该队列事件的完整历史。人员调离队列后立即失去访问权；需要客户数据隔离时通过拆分咨询队列实现，不做逐事件个人授权。

### Q26-Q30

26. **当前责任研发团队共享查看事件时间线。** 当前 `dev_team_id` 下的所有成员均可通过 H5 查看咨询正式提交、咨询补充、研发正式反馈以及状态和转交记录。团队负责人展示名不影响可见范围。咨询身份的展示遵循 2026-09-04 修订后的可见规则。
27. **研发访问权只跟随当前责任团队。** 跨团队转出后，原研发团队立即失去 H5 详情和后续附件访问权，并停止收到后续推送；原群中已经投递的历史消息无法收回。事件重新转回时再恢复访问权。
28. **附件必须进入系统自己的存储。** 收到图片、文件或视频后，在企业微信临时地址有效期内下载、解密并转存，以内部文件 ID 关联 `case_id`，保证历史可继续访问。
29. **附件与文字采用完全相同的事件权限。** 不做应用层额外加密、不建立附件级 ACL，也不设计单独鉴权流程；附件访问复用 H5 会话和事件权限检查，不生成永久公开文件地址。
30. **错绑消息采用追加式纠错。** 原事件保留误发记录并标记“已作废/误发”，正确事件追加正式纠正消息；时间线记录操作者、时间以及原事件和目标事件，已经收到误发内容的一侧会收到简短更正通知。不得静默移动、覆盖或删除原记录。

### Q31-Q35

31. **研发侧区分当前咨询经办人与历史消息作者。** 事件顶部展示当前咨询经办人；每条咨询消息保留并展示当时的真实发送人；交接不会改写旧消息作者，时间线明确展示经办人变更。
32. **研发身份按团队呈现。** 事件详情展示研发责任团队及其纯文本负责人展示名；正式研发反馈展示实际执行发送的人。转发到研发群的消息不指定个人经办人；新建事件消息只标明研发处理团队。
33. **事件状态拆分为生命周期和下一步责任两个维度。** `lifecycle_status` 仅取 `open` / `closed`；`waiting_on` 取 `consult` / `dev` / `none`。例如研发交付解决结果后是 `open + waiting_on=consult`，咨询确认闭环后是 `closed + none`。
34. **正式跨端消息区分“回复并交给对方”和“仅同步进展”。** 前者是默认行为，投递成功后把 `waiting_on` 切换到接收方；后者只追加并投递消息，不改变 `waiting_on`。界面不要求用户理解状态字段，只提供低频使用的“仅同步”选项。
35. **采用先持久化、后可靠投递。** 正式消息依次经历“已接收 → 待投递 → 已投递”；失败自动重试，多次失败后标记失败并通知原发送人。只有企业微信确认投递成功后才切换 `waiting_on`。入站按回调 `msgid` 去重，出站按内部投递记录避免重复转发。

### Q36-Q40

36. **研发路由采用两级映射。** `dev_team_id → 研发群 chatid`。研发团队管理员在群内通过机器人绑定团队与群；团队换群时无需逐个修改人员路由。
37. **团队换群属于通道迁移，不属于事件转交。** 同一研发团队重新绑定新群后，全部未关闭事件自动使用新 `chatid`；旧群停止接收，新群仅收到迁移摘要和事件中心入口，不逐条重放事件。
38. **业务团队使用系统内部稳定 ID。** `dev_team_id` 和 `consult_queue_id` 不直接复用企业微信部门 ID；外部部门仅作为人员同步来源或映射项，组织改名、合并或重建不会改写历史团队身份。
39. **研发人员移动不会自动搬迁事件。** 团队成员调岗不改变事件的 `dev_team_id`，也不触发个人接替流程；确需改变责任团队时执行跨团队转交。
40. **重复确认，不新增规则。** “一件可独立负责、独立关闭的问题对应一个事件”已包含在 Q16 的显式新建规则中。机器人可以提示拆分，但最终必须由咨询显式新建；不得自动拆分自然语言。

### MVP 范围修订

- **MVP 必须支持图文混排。** 2026-09-04 修订：第一版同时接收 `text` 和 `mixed`，保持图文片段的原始顺序，并及时转存其中的图片。独立图片、语音、文件和视频仍可后置。

## 企业微信官方能力约束

来源：[智能机器人开发：使用 URL 回调接收消息](https://developer.work.weixin.qq.com/document/path/101842)，页面标注最后更新于 2026-08-26。

1. 群聊中，文本、图文混排和引用消息只有在用户 `@智能机器人` 时才触发消息回调；单聊中向机器人发送支持的消息即可触发。
2. 回调通用字段包含 `msgid`、`aibotid`、群聊场景下的 `chatid`、`chattype`、`from.userid` 和临时 `response_url`。
3. `msgid` 是本次回调的唯一标志；网络原因可能造成重复回调，因此接收端必须按 `msgid` 幂等去重。
4. 官方文档中的 `quote` 结构只有被引用消息的类型和内容，没有被引用消息的唯一 ID。因此不能实现“沿引用消息 ID 链逐级追溯事件”。实施时必须让可被引用的机器人消息携带可解析的 `case_id`，或者提供另一种明确选择事件的机制。
5. 群聊支持触发回调的内容范围小于单聊：图片、语音、本地文件、视频的独立消息仅支持单聊；群聊可以通过 `@智能机器人` 发送文本、图文混排及引用消息。
6. 图片、文件和视频回调给出的资源 URL 仅五分钟有效，且资源经过加密。若这些内容要进入完整历史，后台必须在有效期内下载、解密并转存，不能只保存原 URL。
7. 如果智能机器人创建者不是企业超级管理员，`from.userid` 是企业主体下的加密用户 ID。身份映射设计不能默认总能取得明文 userid。
8. 接收消息和被动回复均使用加密报文；URL、Token 和 Encoding-AESKey 属于必须受保护的密钥材料。
9. [URL 回调模式的主动回复](https://developer.work.weixin.qq.com/document/path/101845)只能使用当前交互返回的 `response_url` 回复原会话；每个 URL 只能调用一次，有效期为一小时。因此不能仅靠该机制完成从咨询私聊向研发群、再向另一位咨询人员私聊的跨会话路由。
10. [长连接模式的主动推送](https://developer.work.weixin.qq.com/document/path/101837)可以在没有当前用户消息触发的情况下，按用户 `userid` 或群 `chatid` 推送；前提是目标用户或群此前向机器人发送过消息。
11. 长连接模式对同一会话的回复与主动推送合计限制为每分钟 30 条、每小时 1000 条。主动推送请求应使用自行生成的唯一 `req_id`，以便进行请求跟踪及幂等控制。
12. [长连接回复消息](https://developer.work.weixin.qq.com/document/path/101836)支持在模板卡片事件回调后更新卡片，但必须在收到事件后的五秒内完成；不能把初始卡片当作可在任意未来时刻原地更新的永久状态看板。
13. [模板卡片类型](https://developer.work.weixin.qq.com/document/path/101839)支持按钮事件和 URL 跳转，可用“查看事件”按钮打开 H5 详情页。
14. [企业微信网页授权](https://developer.work.weixin.qq.com/document/path/91022)支持通过 `snsapi_base` 静默取得成员基础身份 `userid`。H5 必须在服务端用该身份进行队列权限校验，不能信任 URL 传入的用户身份。

## 已识别但尚未决定

- 机器关联短编号的具体编码、校验和展示格式。
- 研发人员与群聊映射的创建和失效同步方式。
- 重复事件的合并规则。
- 历史文字与附件的保存期限和删除规则。
- 可靠投递的重试次数、退避间隔和最终失败后的人工处理入口。

## 基线更新记录

- 2026-09-03：补录 Q1-Q5。
- 2026-09-03：补录 Q6-Q10。
- 2026-09-03：固化 Q11-Q15，并加入企业微信接收消息、URL 回调主动回复及长连接主动推送的官方文档约束。
- 2026-09-03：固化 Q16-Q20，明确新建/跟进边界、关闭/重开规则及研发侧人员流动模型。
- 2026-09-03：固化 Q21-Q25，明确跨团队转交、群聊低噪声策略、H5 双入口及咨询队列权限。
- 2026-09-04：固化 Q26-Q30，明确研发时间线权限、附件处理及追加式纠错。
- 2026-09-04：按用户修订，研发侧可以看到当前咨询经办人，取代此前的咨询匿名规则。
- 2026-09-04：固化 Q31-Q35，明确双方身份展示、双维度状态、跨端消息语义和可靠投递。
- 2026-09-04：固化 Q36-Q40；Q40 标记为对既有显式新建规则的重复确认，并停止 grilling、转入 MVP 实施方案编写。
- 2026-09-04：扩大 MVP 内容范围，纳入文本与图片混排消息。
